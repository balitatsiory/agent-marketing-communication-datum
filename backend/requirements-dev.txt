# Outils de développement et de test.
# Installation : pip install -r requirements.txt -r requirements-dev.txt

-r requirements.txt

ruff>=0.5              # mise en forme et analyse statique
mypy>=1.10             # vérification des types
pytest>=8.2
pytest-asyncio>=0.23   # tests des fonctions async
