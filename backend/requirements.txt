# Dépendances de l'API IAGORA.
# Les bornes sont basses : on fige les versions exactes le jour de la mise en production.
# Installation : pip install -r requirements.txt

# --- API ---
fastapi>=0.115
uvicorn[standard]>=0.30

# --- validation et configuration ---
pydantic>=2.7
pydantic-settings>=2.3
email-validator>=2.1          # validation des adresses e-mail (EmailStr)

# --- base de données ---
sqlalchemy[asyncio]>=2.0
asyncpg>=0.29                 # pilote PostgreSQL asynchrone
alembic>=1.13

# --- sécurité ---
bcrypt>=4.1                   # hachage des mots de passe
pyjwt>=2.8                    # jetons d'accès et de renouvellement
cryptography>=42.0            # chiffrement des jetons Meta (Fernet)

# --- appels sortants (Meta, n8n, LLM) ---
httpx>=0.27
